<% if @photo.new_record? %>
  alert("Failed to upload painting: <%= j @photo.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#user-photos").append("<%= j render @photo %>");
  $('#picture_refreshable-<%= @photo.id %>').fadeIn('slow');
<% end %>
