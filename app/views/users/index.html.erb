<% provide(:title, 'Everyone') %>
<h1>Everyone</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag @user.avatar.url(:thumb), class: "drop-shadow" %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>